DECLARE 
	V_USER_ID USERS.USER_ID%TYPE;
	V_USERNAME USERS.USERNAME%TYPE;
	V_PASSWORD USERS.PASSWORD%TYPE;
	V_EMAIL USERS.EMAIL%TYPE;
	V_RIGSTR_DATE USERS.REGISTRATION_DATE%TYPE;
BEGIN
	SELECT NVL(MAX(USER_ID),0)+1 INTO V_USER_ID FROM USERS;
	V_USERNAME := :USERS.USERNAME;
	V_PASSWORD := :USERS.PASSWORD;
	V_EMAIL := :USERS.EMAIL;
	V_RIGSTR_DATE := :USERS.REGISTRATION_DATE;
	
	IF :USERS.EMAIL IS NOT NULL THEN 
		INSERT INTO USERS VALUES(V_USER_ID,
															V_USERNAME,
															V_PASSWORD,
															V_EMAIL,
															V_RIGSTR_DATE);
		COMMIT;
	MESSAGE('Êã ÇáÍÝÙ ÈäÌÇÍ.');
	MESSAGE('Êã ÇáÍÝÙ ÈäÌÇÍ.');
	ELSE 
		INSERT INTO USERS(USER_ID,
											USERNAME,
											PASSWORD,
											REGISTRATION_DATE)
											 VALUES(V_USER_ID,
															V_USERNAME,
															V_PASSWORD,
															V_RIGSTR_DATE);
		COMMIT;
	MESSAGE('Êã ÇáÍÝÙ ÈäÌÇÍ.');
	MESSAGE('Êã ÇáÍÝÙ ÈäÌÇÍ.');
	END IF;
	:USERS.USERNAME := '';
END;